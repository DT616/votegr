# Only needed to rebuild data, and only by these scripts:
#   refresh_addresses.py    requests + shapely
#   refresh_precincts.py    requests + shapely
#   build_graph_chunks.py   shapely
#   build_precincts.py      shapely
#   geocode_places.py       requests (and node, for the centreline pass)
# Every other refresh_/build_ script is standard library only, and the site
# itself needs nothing installed at all.
requests>=2.31
shapely>=2.0
